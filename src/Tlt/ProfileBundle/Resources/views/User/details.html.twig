{% extends '::base.html.twig' %}

{% block page %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Profil utilizator <small>Detalii</small></h1>
            <ol class="breadcrumb">
                <li><a href="{{ path('tlt_profile_user_index') }}"><i class="icon-dashboard"></i> Administrare utilizatori</a></li>
                <li class="active"><i class="icon-file-alt"></i> Profil utilizator</li>
            </ol>
        </div>
    </div><!-- /.row -->


    <div id="dialog" style="display:none;" title="Dialog Title"></div>


    <div class="table-responsive">
        <table class="table table-hover table-striped tablesorter">
            <tbody>
                <tr>
                    <td>Nume de utilizator:</td>
                    <td>{{ user.username }}</td>
                </tr>
                <tr>
                    <td>Status:</td>
                    <td>{{ user.status ? 'Activ' : 'Inactiv' }}</td>
                </tr>
                <tr>
                    <td>Nume:</td>
                    <td>{{ user.lastname }}</td>
                </tr>
                <tr>
                    <td>Prenume:</td>
                    <td>{{ user.firstname }}</td>
                </tr>
                <tr>
                    <td>Adresa de e-mail:</td>
                    <td>{{ user.email }}</td>
                </tr>

                {% if user.branches|length>1 %}
                    <tr>
                        <td rowspan="{{ user.branches|length + 1}}">Agentia/Centrul:</td>
                    </tr>
                        {% for branch in user.branches %}
                            <tr>
                                <td>{{ branch.name }}</td>
                            </tr>
                        {% endfor %}
                {% else %}
                    <tr>
                        <td>Agentia/Centrul:</td>
                        <td>{{ user.branches.first.name }}</td>
                    </tr>
                {% endif %}

                {% if user.owners|length>1 %}
                    <tr>
                        <td rowspan="{{ user.owners|length + 1}}">Entitatea:</td>
                    </tr>
                    {% for owner in user.owners %}
                        <tr>
                            <td>{{ owner.name }}</td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <tr>
                        <td>Entitatea:</td>
                        <td>{{ user.owners.first.name }}</td>
                    </tr>
                {% endif %}

                {% if user.departments|length>1 %}
                    <tr>
                        <td rowspan="{{ user.departments|length+1}}">Tip serviciu:</td>
                    </tr>
                    {% for department in user.departments %}
                        <tr>
                            <td>{{ department.name }}</td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <tr>
                        <td>Tip serviciu:</td>
                        <td>{{ user.departments.first.name }}</td>
                    </tr>

                {% endif %}
                <tr>
                    <td>Notificare pe email:</td>
                    <td>{{ user.emailNotification ? 'da' : 'nu' }}</td>
                </tr>
            </tbody>
            <tfoot>
            <tr>
                <th colspan="2"></th>
            </tr>
            </tfoot>
        </table>
    </div>

{% endblock %}