{% extends '::base.html.twig' %}

{% block page%}
    <h1>Categorii Sisteme - editare</h1>

{#{{ form(edit_form) }} #}

    <div class="form-group">
        {{ form_label(edit_form.department) }}
        {{ form_widget(edit_form.department, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(edit_form.name) }}
        {{ form_widget(edit_form.name, {'attr': {'class': 'form-control', 'placeholder':'introduceti denumirea'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(edit_form.submit) }}
        {{ form_widget(edit_form.submit)}}
    </div>


    <div class="form-group">
        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_systemcategory_index') }}">
            Inapoi la Lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
</div>
{% endblock %}
